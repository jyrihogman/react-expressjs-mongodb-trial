# react-expressjs-mongodb-trial
Testing out ExpressJS W/ TypeScript and MongoDB, repo is by no means ready or cleaned up

To run:
- docker-compose up -d
- ./dockerScript.sh

- npm run startapi
- npm start